// import * as types from '~/shared/actionTypes';
const initialState = {
  isLoggenIn: false,
};

function auth (state = initialState, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default auth;
